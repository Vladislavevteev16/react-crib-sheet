[
  {
    "path": "components",
    "name": "Components",
    "description": [
      {
        "type": "paragraph",
        "content": "Компоненты — это фундаментальные строительные блоки React-приложений. Они инкапсулируют логику и внешний вид части пользовательского интерфейса. Каждый компонент представляет собой независимую, переиспользуемую единицу кода, которую можно комбинировать с другими компонентами для создания сложных интерфейсов."
      },
      {
        "type": "paragraph",
        "content": "React поддерживает два типа компонентов: функциональные и классовые. Функциональные компоненты — это простые JavaScript-функции, которые принимают пропсы и возвращают JSX. Они стали стандартом после появления хуков в React 16.8. Классовые компоненты — это ES6 классы, расширяющие React.Component, с методами жизненного цикла и доступом к this.state."
      },
      {
        "type": "code",
        "content": "// Функциональный компонент (современный подход)\nfunction WelcomeMessage({ userName, isPremium }) {\n  return (\n    <div className=\"welcome-container\">\n      <h1>Welcome, {userName}!</h1>\n      {isPremium && <span className=\"premium-badge\">🌟 PREMIUM</span>}\n    </div>\n  );\n}\n\n// Классовый компонент (legacy код)\nclass UserProfile extends React.Component {\n  constructor(props) {\n    super(props);\n    this.state = { isEditing: false };\n  }\n  \n  render() {\n    return (\n      <div>\n        <h2>{this.props.userName}</h2>\n        <button onClick={() => this.setState({ isEditing: true })}>\n          Edit Profile\n        </button>\n      </div>\n    );\n  }\n}"
      },
      {
        "type": "paragraph",
        "content": "Компоненты следуют принципу единственной ответственности — каждый компонент должен решать одну конкретную задачу. Это делает код более поддерживаемым и тестируемым. Мелкие компоненты легче понимать, отлаживать и переиспользовать в разных частях приложения."
      },
      {
        "type": "list",
        "items": [
          "Функциональные компоненты — рекомендуемый подход, используют хуки",
          "Классовые компоненты — устаревшие, но встречаются в legacy-проектах",
          "Компоненты можно вкладывать друг в друга (композиция)",
          "Каждый компонент должен быть максимально независимым",
          "Имена компонентов начинаются с заглавной буквы (PascalCase)"
        ]
      },
      {
        "type": "paragraph",
        "content": "Композиция компонентов — это ключевой принцип React. Вместо создания монолитных компонентов, вы разделяете UI на мелкие специализированные компоненты, а затем комбинируете их. Например, вместо одного большого компонента UserCard, можно создать отдельные компоненты Avatar, UserInfo, ActionButtons."
      },
      {
        "type": "code",
        "content": "// Композиция компонентов\nfunction UserCard({ user }) {\n  return (\n    <div className=\"user-card\">\n      <Avatar src={user.avatarUrl} size=\"large\" />\n      <UserInfo name={user.name} email={user.email} />\n      <ActionButtons userId={user.id} />\n    </div>\n  );\n}\n\n// Специализированные компоненты\nfunction Avatar({ src, size }) {\n  return <img src={src} className={`avatar avatar-${size}`} />;\n}\n\nfunction UserInfo({ name, email }) {\n  return (\n    <div className=\"user-info\">\n      <h3>{name}</h3>\n      <p>{email}</p>\n    </div>\n  );\n}\n\nfunction ActionButtons({ userId }) {\n  const handleFollow = () => console.log(`Following user ${userId}`);\n  const handleMessage = () => console.log(`Messaging user ${userId}`);\n  \n  return (\n    <div className=\"action-buttons\">\n      <button onClick={handleFollow}>Follow</button>\n      <button onClick={handleMessage}>Message</button>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Когда использовать функциональные компоненты, а когда классовые? После выхода хуков в React 16.8, функциональные компоненты могут делать всё то же, что и классовые, поэтому они стали стандартом. Классовые компоненты нужно знать только для поддержки legacy-кода или если вы работаете с проектами, которые ещё не перешли на хуки."
      },
      {
        "type": "note",
        "content": "Функциональные компоненты обычно короче, проще для понимания и тестирования. Они не имеют экземпляра (this), что устраняет распространённые ошибки с привязкой контекста. Все новые проекты следует разрабатывать с использованием функциональных компонентов и хуков."
      }
    ]
  },
  {
    "path": "props",
    "name": "Props",
    "description": [
      {
        "type": "paragraph",
        "content": "Props (свойства) — это механизм передачи данных от родительских компонентов к дочерним. Это основной способ коммуникации между компонентами в React. Все пропсы собираются в единый объект, который передаётся компоненту в качестве первого аргумента. Пропсы доступны только для чтения (immutable) — компонент никогда не должен изменять свои пропсы напрямую."
      },
      {
        "type": "paragraph",
        "content": "Правило неизменяемости пропсов известно как «чистота» компонентов. Компонент, который не изменяет свои пропсы и всегда возвращает одинаковый результат для одинаковых пропсов, называется чистым (pure). Чистые компоненты предсказуемы, их легче тестировать и отлаживать."
      },
      {
        "type": "code",
        "content": "// Простой пример передачи пропсов\nfunction UserCard({ name, age, isActive, onUserClick }) {\n  return (\n    <div \n      className={`user-card ${isActive ? 'active' : 'inactive'}`}\n      onClick={() => onUserClick(name)}\n    >\n      <h3>{name}</h3>\n      <p>Age: {age}</p>\n      <p>Status: {isActive ? 'Active' : 'Inactive'}</p>\n    </div>\n  );\n}\n\n// Родительский компонент передаёт пропсы\nfunction UserList() {\n  const users = [\n    { id: 1, name: 'Anna', age: 25, isActive: true },\n    { id: 2, name: 'John', age: 30, isActive: false },\n    { id: 3, name: 'Maria', age: 28, isActive: true }\n  ];\n  \n  const handleUserClick = (userName) => {\n    console.log(`User ${userName} clicked`);\n  };\n  \n  return (\n    <div className=\"user-list\">\n      {users.map(user => (\n        <UserCard\n          key={user.id}\n          name={user.name}\n          age={user.age}\n          isActive={user.isActive}\n          onUserClick={handleUserClick}\n        />\n      ))}\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Пропсы могут быть любого типа данных JavaScript: примитивы (строки, числа, булевы значения), объекты, массивы, функции и даже другие React-элементы или компоненты. Функции как пропсы (часто называемые колбэками) позволяют дочерним компонентам взаимодействовать с родительскими — это основной способ «поднятия состояния» (lifting state up)."
      },
      {
        "type": "list",
        "items": [
          "Пропсы доступны только для чтения — нельзя изменять this.props в классах или параметр props в функциях",
          "Поток данных однонаправленный — от родителя к потомку (top-down data flow)",
          "Деструктуризация пропсов улучшает читаемость кода",
          "Пропс children содержит содержимое между открывающим и закрывающим тегами",
          "PropTypes или TypeScript помогают проверять типы пропсов"
        ]
      },
      {
        "type": "paragraph",
        "content": "Специальный пропс children позволяет компонентам принимать произвольное содержимое. Это мощный паттерн для создания компонентов-контейнеров, которые могут рендерить разный контент внутри себя. Дочерние элементы могут быть строками, JSX, другими компонентами или даже функциями (render props)."
      },
      {
        "type": "code",
        "content": "// Пропс children и деструктуризация\nfunction Card({ title, children, theme = 'light' }) {\n  return (\n    <div className={`card card-${theme}`}>\n      {title && <h3 className=\"card-title\">{title}</h3>}\n      <div className=\"card-content\">\n        {/* children содержит всё, что между <Card> и </Card> */}\n        {children}\n      </div>\n    </div>\n  );\n}\n\n// Использование children\nfunction Dashboard() {\n  return (\n    <div>\n      <Card title=\"User Statistics\">\n        {/* Этот контент становится children */}\n        <p>Total posts: 42</p>\n        <p>Followers: 1250</p>\n        <p>Following: 89</p>\n      </Card>\n      \n      <Card title=\"Recent Activity\" theme=\"dark\">\n        <ul>\n          <li>Posted new photo</li>\n          <li>Liked a comment</li>\n          <li>Followed new user</li>\n        </ul>\n      </Card>\n      \n      <Card>\n        {/* Карточка без заголовка, только children */}\n        <p>This card has no title but has content</p>\n      </Card>\n    </div>\n  );\n}\n\n// Spread оператор для пропсов\nfunction Button({ type = 'button', children, ...restProps }) {\n  // restProps содержит все остальные пропсы (onClick, disabled, className и т.д.)\n  return (\n    <button type={type} {...restProps}>\n      {children}\n    </button>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Валидация пропсов — важная практика для больших приложений. React предоставляет встроенную поддержку проверки типов через PropTypes (отдельная библиотека) или TypeScript. Валидация помогает выявлять ошибки на раннем этапе, документирует ожидаемые пропсы и улучшает опыт разработки."
      },
      {
        "type": "code",
        "content": "// Валидация пропсов с PropTypes\nimport PropTypes from 'prop-types';\n\nfunction ProductCard({ product, onAddToCart, isFeatured }) {\n  // Компонентная логика\n  return (\n    <div>\n      <h3>{product.name}</h3>\n      <button onClick={() => onAddToCart(product.id)}>\n        Add to Cart\n      </button>\n    </div>\n  );\n}\n\nProductCard.propTypes = {\n  // Объект product с определённой структурой\n  product: PropTypes.shape({\n    id: PropTypes.number.isRequired,\n    name: PropTypes.string.isRequired,\n    price: PropTypes.number.isRequired,\n    inStock: PropTypes.bool\n  }).isRequired,\n  \n  // Функция, обязательная\n  onAddToCart: PropTypes.func.isRequired,\n  \n  // Необязательный булевый пропс со значением по умолчанию\n  isFeatured: PropTypes.bool\n};\n\nProductCard.defaultProps = {\n  isFeatured: false\n};\n\n// TypeScript альтернатива\ninterface ProductCardProps {\n  product: {\n    id: number;\n    name: string;\n    price: number;\n    inStock?: boolean;\n  };\n  onAddToCart: (productId: number) => void;\n  isFeatured?: boolean;\n}\n\nfunction ProductCardTS({ product, onAddToCart, isFeatured = false }: ProductCardProps) {\n  // Та же логика, но с TypeScript типами\n}"
      },
      {
        "type": "warning",
        "content": "Никогда не изменяйте пропсы внутри компонента. Если компоненту нужно изменять данные, они должны быть частью его состояния или храниться в родительском компоненте и передаваться через пропсы. Нарушение этого правила приведёт к непредсказуемому поведению приложения."
      },
      {
        "type": "note",
        "content": "Деструктуризация пропсов в параметрах функции — лучшая практика. Она делает код чище и позволяет сразу видеть, какие пропсы ожидает компонент. Значения по умолчанию можно задавать через параметры по умолчанию или оператор нулевого слияния (??)."
      }
    ]
  },
  {
    "path": "state",
    "name": "State",
    "description": [
      {
        "type": "paragraph",
        "content": "Состояние (state) — это внутренние данные компонента, которые могут изменяться со временем в ответ на действия пользователя, сетевые запросы, таймеры или другие события. В отличие от пропсов, которые передаются извне и доступны только для чтения, состояние полностью контролируется и управляется самим компонентом. Изменение состояния вызывает повторный рендеринг компонента и его дочерних элементов (если изменение влияет на их рендеринг)."
      },
      {
        "type": "paragraph",
        "content": "Хук useState — это основной способ объявления состояния в функциональных компонентах. Он вызывается на верхнем уровне компонента (не внутри условий, циклов или вложенных функций) и возвращает массив из двух элементов: текущее значение состояния и функцию для его обновления. Имена обычно выбирают по шаблону [something, setSomething]."
      },
      {
        "type": "code",
        "content": "// Базовое использование useState\nfunction Counter() {\n  // Объявление состояния\n  const [count, setCount] = useState(0);\n  \n  // Функция для увеличения счётчика\n  const increment = () => {\n    setCount(count + 1); // Прямое обновление\n  };\n  \n  // Функция для сброса\n  const reset = () => {\n    setCount(0); // Установка конкретного значения\n  };\n  \n  return (\n    <div className=\"counter\">\n      <h2>Count: {count}</h2>\n      <div className=\"buttons\">\n        <button onClick={increment}>Increment</button>\n        <button onClick={reset}>Reset</button>\n      </div>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Важно понимать, что обновление состояния асинхронно. React может группировать несколько вызовов setState для оптимизации производительности. Это означает, что если вы попытаетесь обновить состояние несколько раз подряд, используя текущее значение, вы можете получить неожиданные результаты."
      },
      {
        "type": "code",
        "content": "// Проблема: множественные обновления\nfunction ProblematicCounter() {\n  const [count, setCount] = useState(0);\n  \n  const incrementTwice = () => {\n    // НЕПРАВИЛЬНО: оба вызова используют одно и то же значение count\n    setCount(count + 1); // count = 0 → setCount(1)\n    setCount(count + 1); // count всё ещё = 0 → setCount(1)\n    // В результате count станет 1, а не 2\n  };\n  \n  // Решение: использование функции-колбэка\n  const incrementTwiceCorrect = () => {\n    setCount(prevCount => prevCount + 1); // prevCount = 0 → 1\n    setCount(prevCount => prevCount + 1); // prevCount = 1 → 2\n    // Теперь count станет 2\n  };\n  \n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={incrementTwice}>Increment Twice (Wrong)</button>\n      <button onClick={incrementTwiceCorrect}>Increment Twice (Correct)</button>\n    </div>\n  );\n}"
      },
      {
        "type": "list",
        "items": [
          "Используйте функцию-колбэк при обновлении на основе предыдущего состояния",
          "Состояние должно быть минимальным — храните только необходимые данные",
          "Разделяйте сложное состояние на несколько useState вызовов",
          "Инициализируйте состояние лениво с помощью функции, если вычисление дорогое",
          "Состояние изолировано для каждого экземпляра компонента"
        ]
      },
      {
        "type": "paragraph",
        "content": "Для управления сложными состояниями, особенно когда есть несколько взаимосвязанных значений или сложная логика обновления, можно использовать хук useReducer. Это альтернатива useState, которая заимствует концепцию редюсеров из Redux. useReducer предпочтительнее, когда следующее состояние зависит от предыдущего, или когда есть сложная логика обновления."
      },
      {
        "type": "code",
        "content": "// useReducer для сложного состояния\nfunction todoReducer(state, action) {\n  switch (action.type) {\n    case 'ADD_TODO':\n      return {\n        ...state,\n        todos: [...state.todos, {\n          id: Date.now(),\n          text: action.text,\n          completed: false\n        }]\n      };\n      \n    case 'TOGGLE_TODO':\n      return {\n        ...state,\n        todos: state.todos.map(todo =>\n          todo.id === action.id\n            ? { ...todo, completed: !todo.completed }\n            : todo\n        )\n      };\n      \n    case 'REMOVE_TODO':\n      return {\n        ...state,\n        todos: state.todos.filter(todo => todo.id !== action.id)\n      };\n      \n    case 'SET_FILTER':\n      return {\n        ...state,\n        filter: action.filter\n      };\n      \n    default:\n      return state;\n  }\n}\n\nfunction TodoApp() {\n  const [state, dispatch] = useReducer(todoReducer, {\n    todos: [],\n    filter: 'all'\n  });\n  \n  const filteredTodos = state.todos.filter(todo => {\n    if (state.filter === 'completed') return todo.completed;\n    if (state.filter === 'active') return !todo.completed;\n    return true; // 'all'\n  });\n  \n  return (\n    <div>\n      <h2>Todos ({filteredTodos.length})</h2>\n      <button onClick={() => dispatch({ type: 'SET_FILTER', filter: 'all' })}>\n        All\n      </button>\n      <button onClick={() => dispatch({ type: 'SET_FILTER', filter: 'active' })}>\n        Active\n      </button>\n      <button onClick={() => dispatch({ type: 'SET_FILTER', filter: 'completed' })}>\n        Completed\n      </button>\n      \n      <ul>\n        {filteredTodos.map(todo => (\n          <li key={todo.id}>\n            <span style={{ textDecoration: todo.completed ? 'line-through' : 'none' }}>\n              {todo.text}\n            </span>\n            <button onClick={() => dispatch({ type: 'TOGGLE_TODO', id: todo.id })}>\n              Toggle\n            </button>\n            <button onClick={() => dispatch({ type: 'REMOVE_TODO', id: todo.id })}>\n              Remove\n            </button>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Подъём состояния (lifting state up) — это важный паттерн React, когда состояние перемещается в ближайшего общего родителя компонентов, которым нужно разделять это состояние. Вместо того чтобы пытаться синхронизировать состояние между несколькими компонентами, вы поднимаете его в общего родителя и передаёте вниз через пропсы."
      },
      {
        "type": "code",
        "content": "// Подъём состояния\nfunction TemperatureInput({ scale, temperature, onTemperatureChange }) {\n  return (\n    <fieldset>\n      <legend>Enter temperature in {scale}:</legend>\n      <input\n        value={temperature}\n        onChange={(e) => onTemperatureChange(e.target.value)}\n      />\n    </fieldset>\n  );\n}\n\nfunction TemperatureCalculator() {\n  const [celsius, setCelsius] = useState('');\n  const [fahrenheit, setFahrenheit] = useState('');\n  \n  const handleCelsiusChange = (value) => {\n    setCelsius(value);\n    if (value === '') {\n      setFahrenheit('');\n    } else {\n      setFahrenheit((parseFloat(value) * 9/5 + 32).toFixed(2));\n    }\n  };\n  \n  const handleFahrenheitChange = (value) => {\n    setFahrenheit(value);\n    if (value === '') {\n      setCelsius('');\n    } else {\n      setCelsius(((parseFloat(value) - 32) * 5/9).toFixed(2));\n    }\n  };\n  \n  return (\n    <div>\n      <TemperatureInput\n        scale=\"celsius\"\n        temperature={celsius}\n        onTemperatureChange={handleCelsiusChange}\n      />\n      <TemperatureInput\n        scale=\"fahrenheit\"\n        temperature={fahrenheit}\n        onTemperatureChange={handleFahrenheitChange}\n      />\n    </div>\n  );\n}"
      },
      {
        "type": "warning",
        "content": "Не мутируйте состояние напрямую. Всегда используйте функцию установки состояния (setState или возвращаемую из useState). Для объектов и массивов создавайте новые значения, а не изменяйте существующие. Используйте spread оператор для объектов и map/filter для массивов."
      },
      {
        "type": "note",
        "content": "Ленивая инициализация состояния: если начальное состояние вычисляется дорого, можно передать функцию в useState. Функция будет вызвана только при первом рендере: const [state, setState] = useState(() => expensiveComputation()). Это улучшает производительность при начальном рендере."
      }
    ]
  },
  {
    "path": "lifecycle",
    "name": "Lifecycle",
    "description": [
      {
        "type": "paragraph",
        "content": "Жизненный цикл компонента — это последовательность этапов, через которые проходит компонент от момента создания (монтирования) до удаления из DOM (размонтирования). Понимание жизненного цикла критически важно для правильного управления побочными эффектами, подписками на события, запросами данных и предотвращения утечек памяти."
      },
      {
        "type": "paragraph",
        "content": "В функциональных компонентах жизненный цикл управляется в основном с помощью хука useEffect. Хук useEffect объединяет функциональность нескольких методов жизненного цикла классовых компонентов: componentDidMount, componentDidUpdate и componentWillUnmount."
      },
      {
        "type": "code",
        "content": "// Базовое использование useEffect\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(false);\n  \n  // Эффект для загрузки данных пользователя\n  useEffect(() => {\n    // Эта функция выполнится после каждого рендера\n    console.log('Effect running for userId:', userId);\n    \n    // Функция для загрузки данных\n    const fetchUserData = async () => {\n      if (!userId) return;\n      \n      setLoading(true);\n      try {\n        const response = await fetch(`https://api.example.com/users/${userId}`);\n        const data = await response.json();\n        setUser(data);\n      } catch (error) {\n        console.error('Failed to fetch user:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n    \n    fetchUserData();\n    \n    // Функция очистки (cleanup)\n    return () => {\n      console.log('Cleanup for userId:', userId);\n      // Отмена запросов, удаление слушателей и т.д.\n    };\n  }, [userId]); // Массив зависимостей\n  \n  if (loading) return <div>Loading...</div>;\n  \n  return (\n    <div>\n      {user ? (\n        <div>\n          <h2>{user.name}</h2>\n          <p>Email: {user.email}</p>\n        </div>\n      ) : (\n        <p>No user selected</p>\n      )}\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Массив зависимостей (второй аргумент useEffect) определяет, когда эффект должен выполняться. Если массив пустой [], эффект выполнится только один раз после монтирования (аналог componentDidMount). Если в массиве указаны зависимости [dep1, dep2], эффект выполнится при монтировании и при изменении любой из зависимостей (аналог componentDidUpdate). Если массив не указан вообще, эффект выполнится после каждого рендера."
      },
      {
        "type": "list",
        "items": [
          "componentDidMount → useEffect с пустым массивом []",
          "componentDidUpdate → useEffect с зависимостями [dep1, dep2]",
          "componentWillUnmount → функция возвращаемая из useEffect",
          "shouldComponentUpdate → React.memo, useMemo, useCallback",
          "getDerivedStateFromProps → обновление состояния в теле компонента"
        ]
      },
      {
        "type": "paragraph",
        "content": "Функция очистки (cleanup) возвращается из useEffect и выполняется перед следующим выполнением эффекта или перед размонтированием компонента. Это критически важно для предотвращения утечек памяти: отписка от событий, отмена сетевых запросов, очистка таймеров, удаление слушателей."
      },
      {
        "type": "code",
        "content": "// Использование нескольких эффектов\nfunction DocumentTitleUpdater({ pageTitle }) {\n  // Эффект 1: Обновление заголовка документа\n  useEffect(() => {\n    const originalTitle = document.title;\n    document.title = pageTitle ? `${pageTitle} - My App` : 'My App';\n    \n    return () => {\n      document.title = originalTitle; // Восстанавливаем оригинальный заголовок\n    };\n  }, [pageTitle]); // Зависит от pageTitle\n  \n  // Эффект 2: Подписка на изменение размера окна\n  useEffect(() => {\n    const handleResize = () => {\n      console.log('Window size:', window.innerWidth, 'x', window.innerHeight);\n    };\n    \n    window.addEventListener('resize', handleResize);\n    \n    return () => {\n      window.removeEventListener('resize', handleResize); // Отписка\n    };\n  }, []); // Только при монтировании/размонтировании\n  \n  // Эффект 3: Таймер\n  useEffect(() => {\n    const intervalId = setInterval(() => {\n      console.log('Timer tick');\n    }, 1000);\n    \n    return () => {\n      clearInterval(intervalId); // Очистка таймера\n    };\n  }, []);\n  \n  return <h1>{pageTitle}</h1>;\n}"
      },
      {
        "type": "paragraph",
        "content": "useLayoutEffect — это ещё один хук, похожий на useEffect, но он выполняется синхронно после всех изменений DOM, но перед тем как браузер отрисует изменения. Это полезно для измерений DOM или мутаций, которые должны быть видны пользователю синхронно. Обычно используется для анимаций или когда нужно измерить элемент перед рендером."
      },
      {
        "type": "code",
        "content": "// useLayoutEffect для синхронных операций с DOM\nfunction Tooltip({ children, targetRef }) {\n  const [position, setPosition] = useState({ top: 0, left: 0 });\n  \n  useLayoutEffect(() => {\n    if (targetRef.current) {\n      const rect = targetRef.current.getBoundingClientRect();\n      \n      // Измеряем положение и устанавливаем состояние СИНХРОННО\n      // чтобы избежать мерцания при рендере\n      setPosition({\n        top: rect.bottom + window.scrollY + 5,\n        left: rect.left + window.scrollX\n      });\n    }\n  }, [targetRef]);\n  \n  return (\n    <div \n      className=\"tooltip\"\n      style={{\n        position: 'absolute',\n        top: `${position.top}px`,\n        left: `${position.left}px`\n      }}\n    >\n      {children}\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "В классовых компонентах (которые всё ещё встречаются в legacy-коде) используются специальные методы жизненного цикла. constructor() вызывается при создании компонента, componentDidMount() — после монтирования в DOM, componentDidUpdate() — после обновления, componentWillUnmount() — перед размонтированием. Также есть shouldComponentUpdate() для оптимизации рендеров."
      },
      {
        "type": "code",
        "content": "// Классовый компонент с методами жизненного цикла\nclass UserProfileClass extends React.Component {\n  constructor(props) {\n    super(props);\n    this.state = { user: null, loading: false };\n    console.log('constructor: компонент создан');\n  }\n  \n  componentDidMount() {\n    console.log('componentDidMount: компонент добавлен в DOM');\n    this.fetchUserData();\n    \n    // Подписка на события\n    window.addEventListener('resize', this.handleResize);\n  }\n  \n  componentDidUpdate(prevProps) {\n    console.log('componentDidUpdate: компонент обновился');\n    \n    // Проверка изменения пропсов\n    if (this.props.userId !== prevProps.userId) {\n      console.log('userId изменился, загружаем новые данные');\n      this.fetchUserData();\n    }\n  }\n  \n  componentWillUnmount() {\n    console.log('componentWillUnmount: компонент будет удалён');\n    \n    // Очистка\n    window.removeEventListener('resize', this.handleResize);\n    // Отмена запросов, очистка таймеров\n  }\n  \n  fetchUserData = async () => {\n    const { userId } = this.props;\n    if (!userId) return;\n    \n    this.setState({ loading: true });\n    \n    try {\n      const response = await fetch(`https://api.example.com/users/${userId}`);\n      const data = await response.json();\n      this.setState({ user: data, loading: false });\n    } catch (error) {\n      console.error('Failed to fetch user:', error);\n      this.setState({ loading: false });\n    }\n  };\n  \n  handleResize = () => {\n    console.log('Window resized');\n  };\n  \n  render() {\n    console.log('render: компонент рендерится');\n    const { user, loading } = this.state;\n    \n    if (loading) return <div>Loading...</div>;\n    \n    return (\n      <div>\n        {user ? (\n          <div>\n            <h2>{user.name}</h2>\n            <p>Email: {user.email}</p>\n          </div>\n        ) : (\n          <p>No user data</p>\n        )}\n      </div>\n    );\n  }\n}"
      },
      {
        "type": "warning",
        "content": "Не забывайте про очистку в useEffect. Утечки памяти — распространённая проблема в React-приложениях. Всегда отписывайтесь от событий, отменяйте запросы и очищайте таймеры в функции очистки. Особенно это важно для компонентов, которые часто монтируются/размонтируются."
      },
      {
        "type": "note",
        "content": "Разделяйте эффекты по назначению. Вместо одного большого useEffect с множеством зависимостей, создавайте несколько useEffect, каждый для своей задачи. Это делает код чище и позволяет лучше контролировать выполнение эффектов."
      }
    ]
  },
  {
    "path": "events",
    "name": "Events",
    "description": [
      {
        "type": "paragraph",
        "content": "React использует синтетическую систему событий — кросс-браузерную обёртку над нативными событиями браузера. События в React именуются в camelCase (onClick, onChange, onKeyDown) и передаются как функции, а не строки. React автоматически передаёт объект синтетического события (SyntheticEvent) в качестве аргумента обработчикам событий."
      },
      {
        "type": "paragraph",
        "content": "Ключевые особенности обработки событий в React: необходимость явного вызова preventDefault() для предотвращения поведения по умолчанию (в отличие от возврата false в обычном JavaScript), делегирование событий на корневой элемент для производительности, пулинг (переиспользование) синтетических событий, что требует вызова event.persist() для асинхронного использования."
      },
      {
        "type": "code",
        "content": "// Базовые обработчики событий\nfunction EventBasics() {\n  const handleClick = (event) => {\n    // SyntheticEvent\n    event.preventDefault(); // Отмена поведения по умолчанию\n    event.stopPropagation(); // Остановка всплытия\n    \n    console.log('Event type:', event.type);\n    console.log('Target:', event.target);\n    console.log('Current target:', event.currentTarget);\n  };\n  \n  const handleChange = (event) => {\n    console.log('Input value:', event.target.value);\n  };\n  \n  const handleKeyDown = (event) => {\n    if (event.key === 'Enter') {\n      console.log('Enter pressed');\n      event.preventDefault();\n    }\n  };\n  \n  return (\n    <div>\n      <button onClick={handleClick}>Click me</button>\n      <input \n        type=\"text\" \n        onChange={handleChange}\n        onKeyDown={handleKeyDown}\n        placeholder=\"Press Enter...\"\n      />\n      <form onSubmit={(e) => {\n        e.preventDefault(); // Критически важно для форм!\n        console.log('Form submitted');\n      }}>\n        <button type=\"submit\">Submit</button>\n      </form>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "React поддерживает широкий спектр событий, которые можно разделить на категории: мышиные события (onClick, onDoubleClick, onMouseEnter, onMouseLeave), клавиатурные события (onKeyDown, onKeyUp, onKeyPress), события форм (onChange, onSubmit, onFocus, onBlur), события касания (onTouchStart, onTouchMove, onTouchEnd), события фокуса (onFocus, onBlur), события скролла (onScroll) и другие."
      },
      {
        "type": "list",
        "items": [
          "События мыши: onClick, onDoubleClick, onMouseEnter, onMouseLeave",
          "Клавиатурные: onKeyDown, onKeyUp, onKeyPress, onKeyUp",
          "Формы: onChange, onSubmit, onFocus, onBlur, onInput",
          "Фокуса: onFocus, onBlur, onFocusIn, onFocusOut",
          "Касания: onTouchStart, onTouchMove, onTouchEnd",
          "Прочие: onScroll, onResize, onLoad, onError"
        ]
      },
      {
        "type": "code",
        "content": "// Всплытие (bubbling) и перехват (capturing) событий\nfunction EventPhases() {\n  return (\n    <div \n      onClick={() => console.log('Outer div (bubbling)')}\n      onClickCapture={() => console.log('Outer div (capturing)')}\n      style={{ padding: '30px', backgroundColor: '#ffcccc' }}\n    >\n      Outer\n      <div \n        onClick={(e) => {\n          console.log('Inner div (bubbling)');\n          // e.stopPropagation(); // Остановит всплытие\n        }}\n        onClickCapture={() => console.log('Inner div (capturing)')}\n        style={{ padding: '20px', backgroundColor: '#ccffcc' }}\n      >\n        Inner\n        <button \n          onClick={() => console.log('Button (bubbling)')}\n          onClickCapture={() => console.log('Button (capturing)')}\n        >\n          Click me\n        </button>\n      </div>\n    </div>\n  );\n}\n\n// Параметры в обработчиках событий\nfunction EventParameters() {\n  const items = ['Apple', 'Banana', 'Cherry'];\n  \n  // Фабрика обработчиков\n  const createClickHandler = (item) => (event) => {\n    console.log(`Clicked on ${item}`);\n    console.log('Event:', event);\n  };\n  \n  return (\n    <ul>\n      {items.map((item, index) => (\n        <li key={index}>\n          <button onClick={createClickHandler(item)}>\n            {item} (Factory)\n          </button>\n          <button onClick={(e) => console.log(`Inline: ${item}`)}>\n            {item} (Inline)\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "События в React пулируются (объединяются в пул) для оптимизации памяти. Объект SyntheticEvent используется повторно, и все его свойства обнуляются после вызова обработчика события. Это означает, что вы не можете использовать событие асинхронно без предварительного вызова event.persist()."
      },
      {
        "type": "code",
        "content": "// Асинхронная работа с событиями\nfunction AsyncEventExample() {\n  const handleAsyncClick = (event) => {\n    // Сохраняем событие для асинхронного использования\n    event.persist();\n    \n    setTimeout(() => {\n      // Теперь event доступен\n      console.log('Async event:', event.type);\n      console.log('Target:', event.target);\n    }, 1000);\n  };\n  \n  const handleClickWithoutPersist = (event) => {\n    setTimeout(() => {\n      // ОШИБКА: event будет null или иметь обнулённые свойства\n      console.log('Event without persist:', event?.type);\n    }, 1000);\n  };\n  \n  return (\n    <div>\n      <button onClick={handleAsyncClick}>Click with persist</button>\n      <button onClick={handleClickWithoutPersist}>Click without persist</button>\n    </div>\n  );\n}\n\n// События клавиатуры с модификаторами\nfunction KeyboardEvents() {\n  const [keyLog, setKeyLog] = useState([]);\n  \n  const handleKeyDown = (event) => {\n    const keyInfo = {\n      key: event.key,\n      code: event.code,\n      ctrl: event.ctrlKey,\n      alt: event.altKey,\n      shift: event.shiftKey,\n      meta: event.metaKey\n    };\n    \n    setKeyLog(prev => [keyInfo, ...prev.slice(0, 5)]);\n    \n    // Горячие клавиши\n    if (event.ctrlKey && event.key === 's') {\n      event.preventDefault();\n      console.log('Ctrl+S: Save document');\n    }\n    \n    if (event.altKey && event.key === 'F4') {\n      console.log('Alt+F4 pressed');\n    }\n  };\n  \n  return (\n    <div>\n      <input \n        type=\"text\"\n        onKeyDown={handleKeyDown}\n        placeholder=\"Type here...\"\n      />\n      <div>\n        <h4>Recent keys:</h4>\n        {keyLog.map((key, i) => (\n          <div key={i}>\n            {key.key} {key.ctrl && 'Ctrl'} {key.alt && 'Alt'} {key.shift && 'Shift'}\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "События форм в React могут быть управляемыми (controlled) или неуправляемыми (uncontrolled). В управляемых компонентах значение элемента формы привязывается к состоянию React. В неуправляемых компонентах используются refs для доступа к DOM-элементам. Управляемые компоненты дают больше контроля и упрощают валидацию."
      },
      {
        "type": "code",
        "content": "// Управляемые формы\nfunction ControlledForm() {\n  const [formData, setFormData] = useState({\n    username: '',\n    email: '',\n    newsletter: false,\n    gender: 'male'\n  });\n  \n  const handleChange = (event) => {\n    const { name, value, type, checked } = event.target;\n    \n    setFormData(prev => ({\n      ...prev,\n      [name]: type === 'checkbox' ? checked : value\n    }));\n  };\n  \n  const handleSubmit = (event) => {\n    event.preventDefault();\n    console.log('Form data:', formData);\n  };\n  \n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        name=\"username\"\n        value={formData.username}\n        onChange={handleChange}\n        placeholder=\"Username\"\n      />\n      \n      <input\n        name=\"email\"\n        type=\"email\"\n        value={formData.email}\n        onChange={handleChange}\n        placeholder=\"Email\"\n      />\n      \n      <label>\n        <input\n          name=\"newsletter\"\n          type=\"checkbox\"\n          checked={formData.newsletter}\n          onChange={handleChange}\n        />\n        Subscribe to newsletter\n      </label>\n      \n      <select \n        name=\"gender\" \n        value={formData.gender} \n        onChange={handleChange}\n      >\n        <option value=\"male\">Male</option>\n        <option value=\"female\">Female</option>\n        <option value=\"other\">Other</option>\n      </select>\n      \n      <button type=\"submit\">Submit</button>\n    </form>\n  );\n}\n\n// События мыши с координатами\nfunction MouseEvents() {\n  const [coords, setCoords] = useState({ x: 0, y: 0 });\n  const [isDragging, setIsDragging] = useState(false);\n  \n  const handleMouseMove = (event) => {\n    setCoords({ x: event.clientX, y: event.clientY });\n  };\n  \n  const handleDragStart = (event) => {\n    setIsDragging(true);\n    console.log('Drag start at:', event.clientX, event.clientY);\n  };\n  \n  const handleDragEnd = () => {\n    setIsDragging(false);\n  };\n  \n  return (\n    <div \n      onMouseMove={handleMouseMove}\n      style={{ height: '200px', border: '1px solid #ccc', position: 'relative' }}\n    >\n      <div style={{ position: 'absolute', top: '10px', left: '10px' }}>\n        Mouse: {coords.x}, {coords.y}\n      </div>\n      \n      <div\n        draggable\n        onDragStart={handleDragStart}\n        onDragEnd={handleDragEnd}\n        style={{\n          width: '100px',\n          height: '100px',\n          backgroundColor: isDragging ? '#ff9999' : '#99ccff',\n          cursor: 'move'\n        }}\n      >\n        Drag me\n      </div>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Делегирование событий — важная техника оптимизации, особенно при работе с большими списками. Вместо добавления обработчика события к каждому элементу списка, вы добавляете один обработчик к родительскому элементу и определяете целевой элемент через event.target. Это улучшает производительность и упрощает управление событиями."
      },
      {
        "type": "code",
        "content": "// Делегирование событий\nfunction EventDelegation() {\n  const listRef = useRef(null);\n  \n  useEffect(() => {\n    const handleListClick = (event) => {\n      // Определяем, был ли клик на элементе списка\n      if (event.target.tagName === 'LI') {\n        const itemId = event.target.dataset.id;\n        console.log('Clicked item:', itemId);\n        \n        // Изменяем стиль кликнутого элемента\n        event.target.style.backgroundColor = '#e0e0e0';\n        \n        setTimeout(() => {\n          event.target.style.backgroundColor = '';\n        }, 300);\n      }\n    };\n    \n    const list = listRef.current;\n    list.addEventListener('click', handleListClick);\n    \n    return () => {\n      list.removeEventListener('click', handleListClick);\n    };\n  }, []);\n  \n  const items = [\n    { id: 1, text: 'Item 1' },\n    { id: 2, text: 'Item 2' },\n    { id: 3, text: 'Item 3' },\n    { id: 4, text: 'Item 4' },\n    { id: 5, text: 'Item 5' }\n  ];\n  \n  return (\n    <div>\n      <h3>Event Delegation Example</h3>\n      <p>Only one event listener for all items</p>\n      \n      <ul ref={listRef} style={{ listStyle: 'none', padding: 0 }}>\n        {items.map(item => (\n          <li \n            key={item.id}\n            data-id={item.id}\n            style={{\n              padding: '10px',\n              margin: '5px 0',\n              border: '1px solid #ddd',\n              cursor: 'pointer'\n            }}\n          >\n            {item.text}\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}\n\n// Кастомные события\nfunction CustomEvents() {\n  const dispatchCustomEvent = () => {\n    // Создание и диспетчеризация кастомного события\n    const event = new CustomEvent('reactapp:notification', {\n      detail: {\n        message: 'Hello from custom event!',\n        timestamp: new Date().toISOString()\n      },\n      bubbles: true,\n      cancelable: true\n    });\n    \n    window.dispatchEvent(event);\n  };\n  \n  useEffect(() => {\n    const handleCustomEvent = (event) => {\n      console.log('Custom event received:', event.detail);\n      alert(`Custom: ${event.detail.message}`);\n    };\n    \n    window.addEventListener('reactapp:notification', handleCustomEvent);\n    \n    return () => {\n      window.removeEventListener('reactapp:notification', handleCustomEvent);\n    };\n  }, []);\n  \n  return (\n    <button onClick={dispatchCustomEvent}>\n      Dispatch Custom Event\n    </button>\n  );\n}"
      },
      {
        "type": "warning",
        "content": "Не забывайте про event.persist() при асинхронной работе с событиями. Без этого события будут обнулены к моменту асинхронного вызова. Также избегайте inline-функций в рендере для часто обновляемых компонентов — они создают новую функцию при каждом рендере, что может вызвать лишние ререндеры дочерних компонентов."
      },
      {
        "type": "note",
        "content": "Используйте пассивные обработчики событий (passive event listeners) для событий скролла и касания для улучшения производительности. В нативных обработчиках можно использовать { passive: true }, но в React SyntheticEvent это делается автоматически для некоторых событий. Для кастомных нативных обработчиков указывайте passive явно."
      }
    ]
  },
  {
    "path": "memo",
    "name": "React.memo",
    "description": [
      {
        "type": "paragraph",
        "content": "React.memo — это компонент высшего порядка (Higher-Order Component), который мемоизирует результат рендеринга компонента. Он предотвращает ненужные повторные рендеры, когда пропсы компонента не изменились. React.memo выполняет поверхностное сравнение (shallow comparison) пропсов, но можно предоставить пользовательскую функцию сравнения вторым аргументом."
      },
      {
        "type": "paragraph",
        "content": "Основная цель React.memo — оптимизация производительности. При каждом обновлении родительского компонента React по умолчанию перерендеривает все дочерние компоненты, даже если их пропсы не изменились. React.memo решает эту проблему, запоминая результат рендеринга и возвращая его при следующих рендерах, если пропсы остались теми же."
      },
      {
        "type": "code",
        "content": "// Базовое использование React.memo\nconst UserProfile = React.memo(function UserProfile({ user, onUpdate }) {\n  console.log('UserProfile rendered'); // Проверяем рендеры\n  \n  return (\n    <div className=\"user-profile\">\n      <h3>{user.name}</h3>\n      <p>Email: {user.email}</p>\n      <button onClick={() => onUpdate(user.id)}>\n        Update User\n      </button>\n    </div>\n  );\n});\n\n// Компонент без React.memo для сравнения\nfunction RegularUserProfile({ user, onUpdate }) {\n  console.log('RegularUserProfile rendered'); // Будет рендериться чаще\n  \n  return (\n    <div className=\"user-profile\">\n      <h3>{user.name}</h3>\n      <p>Email: {user.email}</p>\n      <button onClick={() => onUpdate(user.id)}>\n        Update User\n      </button>\n    </div>\n  );\n}\n\n// Родительский компонент\nfunction UserList() {\n  const [users, setUsers] = useState([\n    { id: 1, name: 'Anna', email: 'anna@example.com' },\n    { id: 2, name: 'John', email: 'john@example.com' },\n  ]);\n  \n  const [counter, setCounter] = useState(0);\n  \n  const handleUpdateUser = useCallback((userId) => {\n    console.log('Updating user:', userId);\n  }, []);\n  \n  return (\n    <div>\n      <h2>Counter: {counter}</h2>\n      <button onClick={() => setCounter(c => c + 1)}>\n        Increment Counter\n      </button>\n      \n      <div>\n        <h3>Memoized Component:</h3>\n        {users.map(user => (\n          <UserProfile \n            key={user.id}\n            user={user}\n            onUpdate={handleUpdateUser}\n          />\n        ))}\n      </div>\n      \n      <div>\n        <h3>Regular Component:</h3>\n        {users.map(user => (\n          <RegularUserProfile \n            key={user.id}\n            user={user}\n            onUpdate={handleUpdateUser}\n          />\n        ))}\n      </div>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "React.memo особенно полезен для компонентов, которые: 1) Часто рендерятся с одинаковыми пропсами; 2) Имеют дорогостоящие вычисления при рендере; 3) Являются чистыми (pure) — одинаковые пропсы всегда дают одинаковый результат. Однако важно понимать, что мемоизация имеет свою стоимость — сравнение пропсов также требует вычислительных ресурсов."
      },
      {
        "type": "code",
        "content": "// Пользовательская функция сравнения для React.memo\nconst ComplexUserProfile = React.memo(function ComplexUserProfile({ \n  user, \n  settings, \n  onAction \n}) {\n  console.log('ComplexUserProfile rendered');\n  \n  // Дорогостоящие вычисления\n  const userStats = calculateUserStats(user);\n  \n  return (\n    <div className=\"complex-user-profile\">\n      <h3>{user.name}</h3>\n      <p>Score: {userStats.score}</p>\n      <p>Level: {userStats.level}</p>\n      <button onClick={() => onAction('message', user.id)}>\n        Send Message\n      </button>\n    </div>\n  );\n}, arePropsEqual); // Второй аргумент — функция сравнения\n\n// Пользовательская функция сравнения пропсов\nfunction arePropsEqual(prevProps, nextProps) {\n  /*\n  Возвращает true, если рендерить не нужно (пропсы равны)\n  Возвращает false, если рендерить нужно (пропсы изменились)\n  */\n  \n  // Сравниваем только важные поля объекта user\n  const userChanged = \n    prevProps.user.id !== nextProps.user.id ||\n    prevProps.user.name !== nextProps.user.name ||\n    prevProps.user.email !== nextProps.user.email;\n  \n  // Игнорируем некоторые поля settings, которые не влияют на рендер\n  const settingsChanged = \n    prevProps.settings.theme !== nextProps.settings.theme ||\n    prevProps.settings.language !== nextProps.settings.language;\n  // Игнорируем settings.notifications, так как они не влияют на UI\n  \n  // Сравниваем функцию onAction по ссылке\n  const onActionChanged = prevProps.onAction !== nextProps.onAction;\n  \n  // Если ничего важное не изменилось — не рендерить\n  return !(userChanged || settingsChanged || onActionChanged);\n}\n\n// Вспомогательная функция (имитация дорогих вычислений)\nfunction calculateUserStats(user) {\n  // Имитация сложных вычислений\n  let score = 0;\n  for (let i = 0; i < 100000; i++) {\n    score += Math.sqrt(i) * user.id;\n  }\n  \n  return {\n    score: Math.round(score),\n    level: Math.floor(user.id / 10) + 1\n  };\n}"
      },
      {
        "type": "list",
        "items": [
          "React.memo предотвращает рендеры при неизменных пропсах",
          "Поверхностное сравнение (shallow comparison) по умолчанию",
          "Можно предоставить кастомную функцию сравнения",
          "Работает только с функциональными компонентами",
          "Не предотвращает рендеры при изменении контекста"
        ]
      },
      {
        "type": "paragraph",
        "content": "Важно понимать разницу между React.memo и useMemo/useCallback. React.memo мемоизирует весь компонент, предотвращая его рендер, тогда как useMemo мемоизирует значение, а useCallback — функцию. Часто их используют вместе: React.memo для компонента, useCallback для функций-колбэков, которые передаются как пропсы."
      },
      {
        "type": "code",
        "content": "// Комбинация React.memo с useCallback и useMemo\nconst OptimizedProductCard = React.memo(function OptimizedProductCard({ \n  product, \n  onAddToCart \n}) {\n  console.log('ProductCard rendered for:', product.name);\n  \n  // useMemo для дорогих вычислений внутри компонента\n  const discountedPrice = useMemo(() => {\n    console.log('Calculating discounted price for:', product.name);\n    return product.price * (1 - product.discount);\n  }, [product.price, product.discount]);\n  \n  return (\n    <div className=\"product-card\">\n      <h3>{product.name}</h3>\n      <p>Price: ${discountedPrice.toFixed(2)}</p>\n      <p>Original: ${product.price.toFixed(2)}</p>\n      <button onClick={() => onAddToCart(product.id)}>\n        Add to Cart\n      </button>\n    </div>\n  );\n});\n\nfunction ProductList() {\n  const [products, setProducts] = useState([\n    { id: 1, name: 'Laptop', price: 999.99, discount: 0.1 },\n    { id: 2, name: 'Mouse', price: 29.99, discount: 0 },\n    { id: 3, name: 'Keyboard', price: 89.99, discount: 0.2 }\n  ]);\n  \n  const [filter, setFilter] = useState('');\n  \n  // useCallback для стабильной ссылки на функцию\n  const handleAddToCart = useCallback((productId) => {\n    console.log('Adding product to cart:', productId);\n    // Логика добавления в корзину\n  }, []); // Пустой массив зависимостей — функция не пересоздаётся\n  \n  // useMemo для фильтрации продуктов\n  const filteredProducts = useMemo(() => {\n    console.log('Filtering products');\n    return products.filter(product =>\n      product.name.toLowerCase().includes(filter.toLowerCase())\n    );\n  }, [products, filter]);\n  \n  return (\n    <div>\n      <input\n        type=\"text\"\n        value={filter}\n        onChange={(e) => setFilter(e.target.value)}\n        placeholder=\"Filter products...\"\n      />\n      \n      <div>\n        {filteredProducts.map(product => (\n          <OptimizedProductCard\n            key={product.id}\n            product={product}\n            onAddToCart={handleAddToCart}\n          />\n        ))}\n      </div>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Когда НЕ следует использовать React.memo: 1) Для простых компонентов, которые быстро рендерятся; 2) Для компонентов, которые часто получают разные пропсы; 3) Если сравнение пропсов сложнее самого рендера; 4) Для компонентов, которые всегда должны рендериться (например, отображающие текущее время). Преждевременная оптимизация может ухудшить производительность."
      },
      {
        "type": "warning",
        "content": "React.memo не предотвращает рендеры, вызванные изменением контекста (Context). Если компонент использует useContext и значение контекста изменилось, компонент перерендерится, даже если он обёрнут в React.memo и его пропсы не изменились. Для оптимизации контекста используйте разделение контекстов или мемоизацию значения контекста."
      },
      {
        "type": "note",
        "content": "Используйте React DevTools Profiler для определения компонентов, которые действительно нуждаются в оптимизации. Не добавляйте React.memo везде «на всякий случай» — это может снизить производительность из-за затрат на сравнение пропсов. Оптимизируйте только те компоненты, которые вызывают реальные проблемы с производительностью."
      }
    ]
  },
  {
    "path": "useсallback",
    "name": "useCallback",
    "description": [
      {
        "type": "paragraph",
        "content": "Хук useCallback возвращает мемоизированную версию функции-колбэка, которая изменяется только при изменении значений в массиве зависимостей. Это оптимизация, которая предотвращает создание новых функций при каждом рендере, сохраняя ссылочную стабильность функции между рендерами."
      },
      {
        "type": "paragraph",
        "content": "Основная проблема, которую решает useCallback: функции, созданные внутри компонента, пересоздаются при каждом рендере. Это означает, что при каждом рендере создаётся новая функция с новой ссылкой в памяти. Если эта функция передаётся как пропс в дочерний компонент, обёрнутый в React.memo, дочерний компонент будет перерендериваться каждый раз, даже если другие пропсы не изменились."
      },
      {
        "type": "code",
        "content": "// Проблема: функция пересоздаётся при каждом рендере\nfunction ProblematicParent() {\n  const [count, setCount] = useState(0);\n  \n  // Эта функция создаётся заново при каждом рендере\n  const handleClick = () => {\n    console.log('Button clicked, count:', count);\n  };\n  \n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(c => c + 1)}>\n        Increment Parent\n      </button>\n      \n      {/* MemoizedChild получает новую ссылку на handleClick каждый раз */}\n      <MemoizedChild onClick={handleClick} />\n    </div>\n  );\n}\n\n// Решение с useCallback\nfunction OptimizedParent() {\n  const [count, setCount] = useState(0);\n  \n  // Мемоизированная функция\n  const handleClick = useCallback(() => {\n    console.log('Button clicked, count:', count);\n  }, [count]); // Пересоздаётся только при изменении count\n  \n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(c => c + 1)}>\n        Increment Parent\n      </button>\n      \n      {/* MemoizedChild получает ту же ссылку, пока count не изменился */}\n      <MemoizedChild onClick={handleClick} />\n    </div>\n  );\n}\n\n// Мемоизированный дочерний компонент\nconst MemoizedChild = React.memo(function MemoizedChild({ onClick }) {\n  console.log('MemoizedChild rendered'); // Проверяем рендеры\n  \n  return (\n    <button onClick={onClick}>\n      Click Me\n    </button>\n  );\n});"
      },
      {
        "type": "paragraph",
        "content": "Массив зависимостей в useCallback работает так же, как в useEffect. Он определяет, когда функция должна быть пересоздана. Если массив пустой [], функция создаётся один раз при монтировании компонента и никогда не изменяется. Если в массиве указаны зависимости [dep1, dep2], функция пересоздаётся при изменении любой из зависимостей."
      },
      {
        "type": "list",
        "items": [
          "useCallback сохраняет ссылку на функцию между рендерами",
          "Пустой массив [] — функция создаётся один раз",
          "Зависимости [dep1, dep2] — пересоздание при изменении зависимостей",
          "Нет массива — пересоздание при каждом рендере (бесполезно)",
          "Важно включать все используемые в функции переменные в зависимости"
        ]
      },
      {
        "type": "code",
        "content": "// Правильное использование зависимостей в useCallback\nfunction UserDashboard() {\n  const [user, setUser] = useState({ name: 'Anna', age: 25 });\n  const [preferences, setPreferences] = useState({ theme: 'light' });\n  \n  // ПРАВИЛЬНО: все используемые переменные в зависимостях\n  const updateUserProfile = useCallback((newName) => {\n    console.log(`Updating user ${user.name} to ${newName}`);\n    console.log(`Current theme: ${preferences.theme}`);\n    \n    setUser(prev => ({ ...prev, name: newName }));\n  }, [user.name, preferences.theme]); // user.name и preferences.theme в зависимостях\n  \n  // ОШИБКА: забыли зависимости\n  const problematicUpdate = useCallback((newName) => {\n    // user.name будет иметь старое значение из замыкания\n    console.log(`Updating user ${user.name} to ${newName}`);\n    setUser(prev => ({ ...prev, name: newName }));\n  }, []); // Пустой массив — функция запомнит первоначальное user.name\n  \n  // Правильно: функция не использует внешние переменные\n  const resetUser = useCallback(() => {\n    setUser({ name: 'Guest', age: 0 });\n  }, []); // Пустой массив — функция стабильна\n  \n  // Правильно: использует только setUser, который стабилен\n  const incrementAge = useCallback(() => {\n    setUser(prev => ({ ...prev, age: prev.age + 1 }));\n  }, []); // setUser стабилен, не нужно включать\n  \n  return (\n    <div>\n      <h2>{user.name}, {user.age} years</h2>\n      <button onClick={() => updateUserProfile('John')}>\n        Change Name to John\n      </button>\n      <button onClick={incrementAge}>\n        Increment Age\n      </button>\n      <button onClick={resetUser}>\n        Reset User\n      </button>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "useCallback особенно важен в нескольких сценариях: 1) Когда функция передаётся в дочерний компонент, обёрнутый в React.memo; 2) Когда функция используется в useEffect и является его зависимостью; 3) Когда функция используется в других useCallback/useMemo как зависимость; 4) Когда функция передаётся как обработчик события, который может быть удалён/добавлен (addEventListener/removeEventListener)."
      },
      {
        "type": "code",
        "content": "// useCallback с различными типами функций\nfunction ComplexComponent() {\n  const [data, setData] = useState([]);\n  const [filter, setFilter] = useState('');\n  const [sortBy, setSortBy] = useState('name');\n  \n  // 1. Обработчик событий с параметрами\n  const handleItemClick = useCallback((itemId, event) => {\n    console.log('Item clicked:', itemId);\n    console.log('Event type:', event.type);\n  }, []); // Не зависит от состояния компонента\n  \n  // 2. Функция с debounce (задержкой)\n  const handleSearch = useCallback(\n    debounce((searchTerm) => {\n      console.log('Searching for:', searchTerm);\n      // API запрос с searchTerm\n    }, 300),\n    [] // debounce функция стабильна\n  );\n  \n  // 3. Функция для сортировки данных\n  const sortData = useCallback((items) => {\n    const sorted = [...items];\n    \n    if (sortBy === 'name') {\n      sorted.sort((a, b) => a.name.localeCompare(b.name));\n    } else if (sortBy === 'date') {\n      sorted.sort((a, b) => new Date(b.date) - new Date(a.date));\n    } else if (sortBy === 'price') {\n      sorted.sort((a, b) => a.price - b.price);\n    }\n    \n    return sorted;\n  }, [sortBy]); // Зависит от sortBy\n  \n  // 4. Функция для мемоизированного вычисления\n  const filteredAndSortedData = useMemo(() => {\n    const filtered = data.filter(item => \n      item.name.toLowerCase().includes(filter.toLowerCase())\n    );\n    return sortData(filtered);\n  }, [data, filter, sortData]); // sortData в зависимостях\n  \n  // 5. Функция, используемая в useEffect\n  const fetchData = useCallback(async () => {\n    try {\n      const response = await fetch(`/api/data?filter=${filter}`);\n      const result = await response.json();\n      setData(result);\n    } catch (error) {\n      console.error('Failed to fetch data:', error);\n    }\n  }, [filter]); // Зависит от filter\n  \n  useEffect(() => {\n    fetchData();\n  }, [fetchData]); // fetchData в зависимостях useEffect\n  \n  return (\n    <div>\n      <input\n        value={filter}\n        onChange={(e) => setFilter(e.target.value)}\n        placeholder=\"Filter...\"\n      />\n      \n      <select value={sortBy} onChange={(e) => setSortBy(e.target.value)}>\n        <option value=\"name\">Name</option>\n        <option value=\"date\">Date</option>\n        <option value=\"price\">Price</option>\n      </select>\n      \n      <ul>\n        {filteredAndSortedData.map(item => (\n          <li key={item.id} onClick={(e) => handleItemClick(item.id, e)}>\n            {item.name} - {item.date} - ${item.price}\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}\n\n// Вспомогательная функция debounce\nfunction debounce(func, delay) {\n  let timeoutId;\n  return function(...args) {\n    clearTimeout(timeoutId);\n    timeoutId = setTimeout(() => func.apply(this, args), delay);\n  };\n}"
      },
      {
        "type": "paragraph",
        "content": "Когда НЕ использовать useCallback: 1) Для простых функций, которые не передаются в memo-компоненты; 2) Когда оптимизация преждевременна и нет проблем с производительностью; 3) Для функций, которые всегда должны быть свежими (например, использующие самое актуальное состояние); 4) Когда создание useCallback сложнее, чем создание самой функции."
      },
      {
        "type": "warning",
        "content": "Распространённая ошибка — забывать добавлять зависимости в массив useCallback. Если функция использует переменные из области видимости компонента (состояние, пропсы, контекст), их необходимо включить в массив зависимостей. Иначе функция будет использовать устаревшие значения из замыкания."
      },
      {
        "type": "note",
        "content": "useCallback(fn, deps) эквивалентен useMemo(() => fn, deps). Оба хука решают похожие задачи, но useMemo мемоизирует результат вычислений, а useCallback — саму функцию. Для функций предпочтительнее useCallback, так как он более семантически понятен."
      }
    ]
  },
  {
    "path": "usememo",
    "name": "useMemo",
    "description": [
      {
        "type": "paragraph",
        "content": "Хук useMemo возвращает мемоизированное значение, которое пересчитывается только при изменении зависимостей. Это оптимизация производительности, которая позволяет избежать дорогостоящих вычислений при каждом рендере компонента, кэшируя результат вычислений между рендерами."
      },
      {
        "type": "paragraph",
        "content": "Основная проблема, которую решает useMemo: некоторые вычисления могут быть ресурсоёмкими (фильтрация больших массивов, сложные математические операции, преобразование данных). Если эти вычисления выполняются при каждом рендере, это может привести к проблемам с производительностью, особенно на мобильных устройствах или при частых обновлениях интерфейса."
      },
      {
        "type": "code",
        "content": "// Проблема: дорогие вычисления при каждом рендере\nfunction ProblematicProductList({ products, filterText }) {\n  // Эти вычисления выполняются ПРИ КАЖДОМ рендере\n  const filteredProducts = products.filter(product =>\n    product.name.toLowerCase().includes(filterText.toLowerCase())\n  );\n  \n  const sortedProducts = [...filteredProducts].sort((a, b) =>\n    a.name.localeCompare(b.name)\n  );\n  \n  const averagePrice = sortedProducts.length > 0\n    ? sortedProducts.reduce((sum, p) => sum + p.price, 0) / sortedProducts.length\n    : 0;\n  \n  return (\n    <div>\n      <p>Products: {sortedProducts.length}</p>\n      <p>Average price: ${averagePrice.toFixed(2)}</p>\n      {/* ... рендер списка ... */}\n    </div>\n  );\n}\n\n// Решение с useMemo\nfunction OptimizedProductList({ products, filterText }) {\n  // Вычисления выполняются ТОЛЬКО при изменении products или filterText\n  const filteredProducts = useMemo(() => {\n    console.log('Filtering products...');\n    return products.filter(product =>\n      product.name.toLowerCase().includes(filterText.toLowerCase())\n    );\n  }, [products, filterText]); // Зависимости\n  \n  const sortedProducts = useMemo(() => {\n    console.log('Sorting products...');\n    return [...filteredProducts].sort((a, b) =>\n      a.name.localeCompare(b.name)\n    );\n  }, [filteredProducts]); // Зависит от filteredProducts\n  \n  const averagePrice = useMemo(() => {\n    console.log('Calculating average price...');\n    return sortedProducts.length > 0\n      ? sortedProducts.reduce((sum, p) => sum + p.price, 0) / sortedProducts.length\n      : 0;\n  }, [sortedProducts]); // Зависит от sortedProducts\n  \n  return (\n    <div>\n      <p>Products: {sortedProducts.length}</p>\n      <p>Average price: ${averagePrice.toFixed(2)}</p>\n      {/* ... рендер списка ... */}\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "useMemo принимает два аргумента: функцию, которая возвращает вычисляемое значение, и массив зависимостей. Значение будет пересчитываться только при изменении хотя бы одной зависимости. Если зависимости не изменились, useMemo возвращает закэшированное значение с предыдущего рендера."
      },
      {
        "type": "list",
        "items": [
          "Дорогостоящие вычисления (фильтрация, сортировка, преобразования)",
          "Создание сложных объектов или массивов для пропсов",
          "Стабилизация ссылок для сравнения в useEffect/useCallback",
          "Оптимизация компонентов, зависящих от вычислений",
          "Предотвращение создания новых объектов при каждом рендере"
        ]
      },
      {
        "type": "code",
        "content": "// Различные сценарии использования useMemo\nfunction Dashboard({ users, startDate, endDate }) {\n  // 1. Дорогостоящие вычисления\n  const activeUsers = useMemo(() => {\n    console.log('Calculating active users...');\n    \n    return users.filter(user => {\n      // Сложная логика фильтрации\n      const lastActive = new Date(user.lastActive);\n      const isInDateRange = lastActive >= startDate && lastActive <= endDate;\n      const hasMinActivity = user.postsCount > 10 || user.commentsCount > 50;\n      \n      return user.isActive && isInDateRange && hasMinActivity;\n    });\n  }, [users, startDate, endDate]); // Все используемые переменные в зависимостях\n  \n  // 2. Создание сложного объекта для пропсов\n  const dashboardStats = useMemo(() => {\n    console.log('Calculating dashboard stats...');\n    \n    const totalUsers = users.length;\n    const activeCount = activeUsers.length;\n    const engagementRate = totalUsers > 0 ? (activeCount / totalUsers) * 100 : 0;\n    \n    // Сложный объект со статистикой\n    return {\n      totalUsers,\n      activeUsers: activeCount,\n      engagementRate: engagementRate.toFixed(2),\n      newUsersThisWeek: users.filter(u => \n        new Date(u.joinDate) > new Date(Date.now() - 7 * 24 * 60 * 60 * 1000)\n      ).length,\n      topContributors: activeUsers\n        .sort((a, b) => b.postsCount - a.postsCount)\n        .slice(0, 5)\n        .map(u => ({ name: u.name, posts: u.postsCount }))\n    };\n  }, [users, activeUsers]); // Зависимости\n  \n  // 3. Стабилизация ссылки для useEffect\n  const fetchConfig = useMemo(() => ({\n    method: 'GET',\n    headers: {\n      'Authorization': `Bearer ${localStorage.getItem('token')}`,\n      'Content-Type': 'application/json'\n    },\n    cache: 'no-cache'\n  }), []); // Пустой массив — объект создаётся один раз\n  \n  useEffect(() => {\n    // fetchConfig стабилен, эффект не будет запускаться лишний раз\n    fetchData('/api/dashboard', fetchConfig);\n  }, [fetchConfig]);\n  \n  // 4. Мемоизация компонентов (редкий случай)\n  const UserStatsChart = useMemo(() => {\n    return function Chart() {\n      // Сложный график на основе данных\n      return <div>Chart component</div>;\n    };\n  }, []); // Компонент создаётся один раз\n  \n  return (\n    <div>\n      <h2>Dashboard Statistics</h2>\n      <p>Total Users: {dashboardStats.totalUsers}</p>\n      <p>Active Users: {dashboardStats.activeUsers}</p>\n      <p>Engagement Rate: {dashboardStats.engagementRate}%</p>\n      \n      <UserStatsChart />\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "useMemo также полезен для предотвращения создания новых объектов или массивов при каждом рендере. В JavaScript объекты и массивы сравниваются по ссылке, а не по значению. Если вы создаёте новый объект при каждом рендере и передаёте его как пропс в memo-компонент, компонент будет перерендериваться каждый раз, даже если значения в объекте те же."
      },
      {
        "type": "code",
        "content": "// Предотвращение создания новых объектов\nfunction UserPreferences({ user, theme }) {\n  // ПРОБЛЕМА: новый объект при каждом рендере\n  // const config = {\n  //   userId: user.id,\n  //   theme: theme,\n  //   preferences: user.preferences\n  // };\n  \n  // РЕШЕНИЕ: мемоизация объекта\n  const config = useMemo(() => ({\n    userId: user.id,\n    theme: theme,\n    preferences: user.preferences,\n    // Вычисляемое свойство\n    isDarkMode: theme === 'dark',\n    // Метод (лучше выносить отдельно)\n    save: () => {\n      console.log('Saving config for user:', user.id);\n    }\n  }), [user.id, theme, user.preferences]); // Зависимости\n  \n  // ПРОБЛЕМА: новый массив при каждом рендере\n  // const menuItems = [\n  //   { label: 'Profile', path: '/profile' },\n  //   { label: 'Settings', path: '/settings' },\n  //   { label: 'Logout', path: '/logout' }\n  // ];\n  \n  // РЕШЕНИЕ: мемоизация массива\n  const menuItems = useMemo(() => {\n    const baseItems = [\n      { label: 'Profile', path: '/profile' },\n      { label: 'Settings', path: '/settings' },\n      { label: 'Logout', path: '/logout' }\n    ];\n    \n    // Добавляем дополнительные пункты на основе user.role\n    if (user.role === 'admin') {\n      return [\n        ...baseItems,\n        { label: 'Admin Panel', path: '/admin' },\n        { label: 'User Management', path: '/admin/users' }\n      ];\n    }\n    \n    return baseItems;\n  }, [user.role]); // Только user.role в зависимостях\n  \n  return (\n    <div>\n      <Navigation menuItems={menuItems} />\n      <PreferencesForm config={config} />\n    </div>\n  );\n}\n\n// Мемоизированный компонент\nconst Navigation = React.memo(function Navigation({ menuItems }) {\n  console.log('Navigation rendered');\n  \n  return (\n    <nav>\n      <ul>\n        {menuItems.map(item => (\n          <li key={item.path}>\n            <a href={item.path}>{item.label}</a>\n          </li>\n        ))}\n      </ul>\n    </nav>\n  );\n});\n\nconst PreferencesForm = React.memo(function PreferencesForm({ config }) {\n  console.log('PreferencesForm rendered');\n  \n  return (\n    <form>\n      <h3>Preferences for User {config.userId}</h3>\n      <p>Theme: {config.theme}</p>\n      <button onClick={config.save}>Save</button>\n    </form>\n  );\n});"
      },
      {
        "type": "paragraph",
        "content": "Когда НЕ использовать useMemo: 1) Вычисления простые и быстрые; 2) Значение используется только один раз; 3) Преждевременная оптимизация без доказанных проблем; 4) Для семантических гарантий (React может «забыть» мемоизированные значения и пересчитать их); 5) Когда стоимость мемоизации больше, чем стоимость вычисления."
      },
      {
        "type": "warning",
        "content": "useMemo — это оптимизация, а не семантическая гарантия. React может в будущих версиях «забывать» некоторые мемоизированные значения и пересчитывать их. Не полагайтесь на useMemo для предотвращения вычислений, которые должны быть семантически стабильными (например, генерация уникальных ID)."
      },
      {
        "type": "note",
        "content": "Правило: сначала пишите код без useMemo, затем используйте React DevTools Profiler для измерения производительности. Добавляйте useMemo только для тех вычислений, которые действительно вызывают проблемы. Преждевременная оптимизация может сделать код сложнее без реальной пользы."
      }
    ]
  },
  {
    "path": "context",
    "name": "Context API",
    "description": [
      {
        "type": "paragraph",
        "content": "Context API — это механизм React для передачи данных через дерево компонентов без необходимости явно передавать пропсы на каждом уровне. Он решает проблему «пропс дриллинга» (prop drilling) — ситуации, когда пропсы передаются через множество промежуточных компонентов, которым эти данные не нужны, только чтобы добраться до глубоко вложенного компонента, который их использует."
      },
      {
        "type": "paragraph",
        "content": "Context состоит из трёх основных частей: 1) createContext() — создаёт объект Context; 2) Context.Provider — компонент, который предоставляет значение контекста для всех потребителей внутри своего поддерева; 3) useContext() — хук для доступа к значению контекста в функциональных компонентах (или Context.Consumer для классовых компонентов)."
      },
      {
        "type": "code",
        "content": "// 1. Создание контекста\nimport React, { createContext, useState, useContext } from 'react';\n\n// Создаём контекст с начальным значением\nconst ThemeContext = createContext({\n  theme: 'light',\n  toggleTheme: () => {},\n});\n\n// 2. Провайдер с состоянием\nfunction ThemeProvider({ children }) {\n  const [theme, setTheme] = useState('light');\n  \n  // Функция для переключения темы\n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  // Значение контекста\n  const value = {\n    theme,\n    toggleTheme,\n    isDark: theme === 'dark'\n  };\n  \n  return (\n    <ThemeContext.Provider value={value}>\n      {children}\n    </ThemeContext.Provider>\n  );\n}\n\n// 3. Кастомный хук для удобного доступа\nfunction useTheme() {\n  const context = useContext(ThemeContext);\n  \n  if (!context) {\n    throw new Error('useTheme must be used within ThemeProvider');\n  }\n  \n  return context;\n}\n\n// 4. Компоненты, использующие контекст\nfunction ThemeToggle() {\n  const { theme, toggleTheme } = useTheme();\n  \n  return (\n    <button onClick={toggleTheme} className={`theme-toggle ${theme}`}>\n      Switch to {theme === 'light' ? 'Dark' : 'Light'} Theme\n    </button>\n  );\n}\n\nfunction Header() {\n  const { theme, isDark } = useTheme();\n  \n  return (\n    <header className={`header ${theme}`}>\n      <h1>My App {isDark ? '🌙' : '☀️'}</h1>\n      <ThemeToggle />\n    </header>\n  );\n}\n\nfunction Content() {\n  const { theme } = useTheme();\n  \n  return (\n    <main className={`content ${theme}`}>\n      <p>Current theme: {theme}</p>\n      <p>This content adapts to the selected theme.</p>\n    </main>\n  );\n}\n\n// 5. Корневой компонент приложения\nfunction App() {\n  return (\n    <ThemeProvider>\n      <div className=\"app\">\n        <Header />\n        <Content />\n        <Footer />\n      </div>\n    </ThemeProvider>\n  );\n}\n\nfunction Footer() {\n  // Footer может не использовать контекст напрямую\n  return <footer>© 2024 My App</footer>;\n}"
      },
      {
        "type": "paragraph",
        "content": "Context идеально подходит для данных, которые можно считать «глобальными» для всего приложения или его части: тема оформления, предпочтения пользователя, данные аутентификации, локализация, состояние корзины покупок, уведомления и т.д. Однако важно не злоупотреблять Context — для несвязанных данных лучше создавать отдельные контексты."
      },
      {
        "type": "list",
        "items": [
          "Создание: const MyContext = createContext(defaultValue)",
          "Провайдер: <MyContext.Provider value={value}>",
          "Потребление: useContext(MyContext) или <MyContext.Consumer>",
          "Значение по умолчанию используется, если нет соответствующего Provider",
          "Провайдеры можно вкладывать друг в друга"
        ]
      },
      {
        "type": "code",
        "content": "// Множественные контексты для разделения ответственности\nconst AuthContext = createContext({\n  user: null,\n  login: () => {},\n  logout: () => {},\n  isAuthenticated: false,\n});\n\nconst NotificationContext = createContext({\n  notifications: [],\n  addNotification: () => {},\n  removeNotification: () => {},\n});\n\nconst SettingsContext = createContext({\n  language: 'en',\n  timezone: 'UTC',\n  updateSettings: () => {},\n});\n\n// Объединённый провайдер\nfunction AppProviders({ children }) {\n  const [user, setUser] = useState(null);\n  const [notifications, setNotifications] = useState([]);\n  const [settings, setSettings] = useState({\n    language: 'en',\n    timezone: 'UTC'\n  });\n  \n  // Аутентификация\n  const login = useCallback((userData) => {\n    setUser(userData);\n    localStorage.setItem('user', JSON.stringify(userData));\n  }, []);\n  \n  const logout = useCallback(() => {\n    setUser(null);\n    localStorage.removeItem('user');\n  }, []);\n  \n  // Уведомления\n  const addNotification = useCallback((message, type = 'info') => {\n    const notification = {\n      id: Date.now(),\n      message,\n      type,\n      timestamp: new Date().toISOString()\n    };\n    \n    setNotifications(prev => [notification, ...prev]);\n    \n    // Автоудаление через 5 секунд\n    setTimeout(() => {\n      removeNotification(notification.id);\n    }, 5000);\n  }, []);\n  \n  const removeNotification = useCallback((id) => {\n    setNotifications(prev => prev.filter(n => n.id !== id));\n  }, []);\n  \n  // Настройки\n  const updateSettings = useCallback((newSettings) => {\n    setSettings(prev => ({ ...prev, ...newSettings }));\n  }, []);\n  \n  // Мемоизация значений для оптимизации\n  const authValue = useMemo(() => ({\n    user,\n    login,\n    logout,\n    isAuthenticated: !!user\n  }), [user, login, logout]);\n  \n  const notificationValue = useMemo(() => ({\n    notifications,\n    addNotification,\n    removeNotification\n  }), [notifications, addNotification, removeNotification]);\n  \n  const settingsValue = useMemo(() => ({\n    ...settings,\n    updateSettings\n  }), [settings, updateSettings]);\n  \n  return (\n    <AuthContext.Provider value={authValue}>\n      <NotificationContext.Provider value={notificationValue}>\n        <SettingsContext.Provider value={settingsValue}>\n          {children}\n        </SettingsContext.Provider>\n      </NotificationContext.Provider>\n    </AuthContext.Provider>\n  );\n}\n\n// Кастомные хуки для каждого контекста\nfunction useAuth() {\n  return useContext(AuthContext);\n}\n\nfunction useNotifications() {\n  return useContext(NotificationContext);\n}\n\nfunction useSettings() {\n  return useContext(SettingsContext);\n}\n\n// Компонент, использующий несколько контекстов\nfunction UserHeader() {\n  const { user, isAuthenticated, logout } = useAuth();\n  const { addNotification } = useNotifications();\n  const { language } = useSettings();\n  \n  const handleLogout = () => {\n    logout();\n    addNotification('Successfully logged out', 'success');\n  };\n  \n  return (\n    <div className=\"user-header\">\n      {isAuthenticated ? (\n        <>\n          <span>Welcome, {user.name}! ({language})</span>\n          <button onClick={handleLogout}>Logout</button>\n        </>\n      ) : (\n        <span>Please log in</span>\n      )}\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Важная особенность Context: при изменении значения контекста все компоненты, которые используют этот контекст (через useContext или Consumer), перерендериваются. Это может привести к проблемам с производительностью, если контекст содержит часто изменяющиеся данные или если много компонентов подписаны на него."
      },
      {
        "type": "code",
        "content": "// Оптимизация контекста с помощью разделения\n// ПЛОХО: один контекст для всего\n// const UserContext = createContext({\n//   user: null,\n//   preferences: {},\n//   notifications: [],\n//   updateUser: () => {},\n//   updatePreferences: () => {},\n//   addNotification: () => {}\n// });\n\n// ХОРОШО: разделённые контексты\nconst UserContext = createContext(null);\nconst UserPreferencesContext = createContext({});\nconst UserNotificationsContext = createContext([]);\n\nfunction OptimizedUserProvider({ children }) {\n  const [user, setUser] = useState(null);\n  const [preferences, setPreferences] = useState({});\n  const [notifications, setNotifications] = useState([]);\n  \n  // Разделяем обновления\n  const updateUser = useCallback((newUser) => {\n    setUser(newUser);\n  }, []);\n  \n  const updatePreferences = useCallback((newPrefs) => {\n    setPreferences(newPrefs);\n  }, []);\n  \n  const addNotification = useCallback((notification) => {\n    setNotifications(prev => [...prev, notification]);\n  }, []);\n  \n  // Мемоизация значений\n  const userValue = useMemo(() => ({\n    user,\n    updateUser\n  }), [user, updateUser]);\n  \n  const preferencesValue = useMemo(() => ({\n    preferences,\n    updatePreferences\n  }), [preferences, updatePreferences]);\n  \n  const notificationsValue = useMemo(() => ({\n    notifications,\n    addNotification\n  }), [notifications, addNotification]);\n  \n  return (\n    <UserContext.Provider value={userValue}>\n      <UserPreferencesContext.Provider value={preferencesValue}>\n        <UserNotificationsContext.Provider value={notificationsValue}>\n          {children}\n        </UserNotificationsContext.Provider>\n      </UserPreferencesContext.Provider>\n    </UserContext.Provider>\n  );\n}\n\n// Компоненты подписываются только на нужные контексты\nfunction UserProfile() {\n  const { user } = useContext(UserContext); // Перерендеривается только при изменении user\n  \n  return <div>{user?.name}</div>;\n}\n\nfunction ThemeSwitcher() {\n  const { preferences, updatePreferences } = useContext(UserPreferencesContext); // Только при изменении preferences\n  \n  return (\n    <select \n      value={preferences.theme} \n      onChange={(e) => updatePreferences({ theme: e.target.value })}\n    >\n      <option value=\"light\">Light</option>\n      <option value=\"dark\">Dark</option>\n    </select>\n  );\n}\n\nfunction NotificationBell() {\n  const { notifications } = useContext(UserNotificationsContext); // Только при изменении notifications\n  \n  return (\n    <div>\n      <span className=\"badge\">{notifications.length}</span>\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Context с useReducer — мощная комбинация для управления сложным состоянием. useReducer предоставляет структурированный способ обновления состояния, а Context делает это состояние доступным во всём приложении. Этот паттерн часто используется как более простая альтернатива Redux для небольших и средних приложений."
      },
      {
        "type": "code",
        "content": "// Context с useReducer (Redux-подобный паттерн)\nconst CartContext = createContext();\n\n// Редюсер для корзины\nfunction cartReducer(state, action) {\n  switch (action.type) {\n    case 'ADD_ITEM':\n      // Проверяем, нет ли уже такого товара\n      const existingItem = state.items.find(item => item.id === action.payload.id);\n      \n      if (existingItem) {\n        // Увеличиваем количество\n        return {\n          ...state,\n          items: state.items.map(item =>\n            item.id === action.payload.id\n              ? { ...item, quantity: item.quantity + 1 }\n              : item\n          ),\n          total: state.total + action.payload.price\n        };\n      } else {\n        // Добавляем новый товар\n        return {\n          ...state,\n          items: [...state.items, { ...action.payload, quantity: 1 }],\n          total: state.total + action.payload.price\n        };\n      }\n      \n    case 'REMOVE_ITEM':\n      const itemToRemove = state.items.find(item => item.id === action.payload);\n      \n      if (!itemToRemove) return state;\n      \n      return {\n        ...state,\n        items: state.items.filter(item => item.id !== action.payload),\n        total: state.total - (itemToRemove.price * itemToRemove.quantity)\n      };\n      \n    case 'UPDATE_QUANTITY':\n      return {\n        ...state,\n        items: state.items.map(item =>\n          item.id === action.payload.id\n            ? { ...item, quantity: action.payload.quantity }\n            : item\n        ),\n        total: state.items.reduce((sum, item) => {\n          const quantity = item.id === action.payload.id \n            ? action.payload.quantity \n            : item.quantity;\n          return sum + (item.price * quantity);\n        }, 0)\n      };\n      \n    case 'CLEAR_CART':\n      return { items: [], total: 0 };\n      \n    default:\n      return state;\n  }\n}\n\nfunction CartProvider({ children }) {\n  const [state, dispatch] = useReducer(cartReducer, { \n    items: [], \n    total: 0 \n  });\n  \n  // Вспомогательные функции\n  const addItem = useCallback((item) => {\n    dispatch({ type: 'ADD_ITEM', payload: item });\n  }, []);\n  \n  const removeItem = useCallback((itemId) => {\n    dispatch({ type: 'REMOVE_ITEM', payload: itemId });\n  }, []);\n  \n  const updateQuantity = useCallback((itemId, quantity) => {\n    dispatch({ type: 'UPDATE_QUANTITY', payload: { id: itemId, quantity } });\n  }, []);\n  \n  const clearCart = useCallback(() => {\n    dispatch({ type: 'CLEAR_CART' });\n  }, []);\n  \n  // Мемоизированное значение контекста\n  const value = useMemo(() => ({\n    ...state,\n    addItem,\n    removeItem,\n    updateQuantity,\n    clearCart,\n    itemCount: state.items.reduce((sum, item) => sum + item.quantity, 0)\n  }), [state, addItem, removeItem, updateQuantity, clearCart]);\n  \n  return (\n    <CartContext.Provider value={value}>\n      {children}\n    </CartContext.Provider>\n  );\n}\n\nfunction useCart() {\n  const context = useContext(CartContext);\n  \n  if (!context) {\n    throw new Error('useCart must be used within CartProvider');\n  }\n  \n  return context;\n}\n\n// Компоненты, использующие корзину\nfunction AddToCartButton({ product }) {\n  const { addItem } = useCart();\n  \n  return (\n    <button onClick={() => addItem(product)}>\n      Add to Cart\n    </button>\n  );\n}\n\nfunction CartSummary() {\n  const { items, total, itemCount } = useCart();\n  \n  return (\n    <div className=\"cart-summary\">\n      <h3>Cart ({itemCount} items)</h3>\n      <p>Total: ${total.toFixed(2)}</p>\n      \n      <ul>\n        {items.map(item => (\n          <li key={item.id}>\n            {item.name} × {item.quantity} = ${(item.price * item.quantity).toFixed(2)}\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}"
      },
      {
        "type": "warning",
        "content": "Context не заменяет полноценную систему управления состоянием для сложных приложений. Для больших приложений с множеством взаимодействующих состояний, middleware, devtools и т.д., лучше использовать специализированные библиотеки: Redux (с Redux Toolkit), MobX, Zustand или Recoil."
      },
      {
        "type": "note",
        "content": "Используйте кастомные хуки (например, useTheme(), useAuth()) для доступа к контексту. Это делает код чище, централизует проверку наличия провайдера и позволяет легко изменить реализацию контекста в будущем. Именуйте контексты с суффиксом Context для ясности."
      }
    ]
  },
  {
    "path": "hoc",
    "name": "HOC",
    "description": [
      {
        "type": "paragraph",
        "content": "Higher-Order Component (HOC) — это продвинутый паттерн React для повторного использования логики компонентов. HOC — это функция, которая принимает компонент и возвращает новый компонент с дополнительными возможностями. Это похоже на функции высшего порядка (higher-order functions) в JavaScript, которые принимают или возвращают другие функции."
      },
      {
        "type": "paragraph",
        "content": "HOC позволяют изолировать общую логику и применять её к разным компонентам без дублирования кода. Вместо того чтобы копировать одну и ту же логику в несколько компонентов, вы создаёте HOC, который «оборачивает» компоненты, добавляя им нужное поведение. Это следует принципу композиции (composition over inheritance)."
      },
      {
        "type": "code",
        "content": "// Базовый пример HOC для логирования\nfunction withLogger(WrappedComponent) {\n  // Возвращаем новый компонент\n  return function WithLoggerComponent(props) {\n    // Логирование при монтировании\n    useEffect(() => {\n      console.log(`Component ${WrappedComponent.displayName || WrappedComponent.name} mounted`);\n      \n      return () => {\n        console.log(`Component ${WrappedComponent.displayName || WrappedComponent.name} unmounted`);\n      };\n    }, []);\n    \n    // Логирование пропсов\n    console.log(`Rendering ${WrappedComponent.displayName || WrappedComponent.name} with props:`, props);\n    \n    // Рендерим обёрнутый компонент со всеми пропсами\n    return <WrappedComponent {...props} />;\n  };\n}\n\n// HOC для отображения загрузки\nfunction withLoading(WrappedComponent) {\n  return function WithLoadingComponent({ isLoading, ...restProps }) {\n    if (isLoading) {\n      return (\n        <div className=\"loading-container\">\n          <div className=\"spinner\"></div>\n          <p>Loading...</p>\n        </div>\n      );\n    }\n    \n    return <WrappedComponent {...restProps} />;\n  };\n}\n\n// HOC для обработки ошибок\nfunction withErrorBoundary(WrappedComponent) {\n  return class WithErrorBoundaryComponent extends React.Component {\n    constructor(props) {\n      super(props);\n      this.state = { hasError: false, error: null };\n    }\n    \n    static getDerivedStateFromError(error) {\n      return { hasError: true, error };\n    }\n    \n    componentDidCatch(error, errorInfo) {\n      console.error('Error caught by HOC:', error, errorInfo);\n    }\n    \n    render() {\n      if (this.state.hasError) {\n        return (\n          <div className=\"error-boundary\">\n            <h3>Something went wrong</h3>\n            <p>{this.state.error?.message}</p>\n            <button onClick={() => this.setState({ hasError: false })}>\n              Try again\n            </button>\n          </div>\n        );\n      }\n      \n      return <WrappedComponent {...this.props} />;\n    }\n  };\n}\n\n// Простые компоненты\nfunction UserProfile({ user }) {\n  return (\n    <div className=\"user-profile\">\n      <h3>{user.name}</h3>\n      <p>{user.email}</p>\n    </div>\n  );\n}\n\nfunction ProductCard({ product }) {\n  return (\n    <div className=\"product-card\">\n      <h3>{product.name}</h3>\n      <p>${product.price}</p>\n    </div>\n  );\n}\n\n// Применение HOC\nconst UserProfileWithLogger = withLogger(UserProfile);\nconst UserProfileWithLoading = withLoading(UserProfile);\nconst UserProfileWithErrorBoundary = withErrorBoundary(UserProfile);\n\n// Комбинация HOC\nconst EnhancedUserProfile = withLogger(withLoading(withErrorBoundary(UserProfile)));\n\n// Использование\nfunction App() {\n  const user = { name: 'Anna', email: 'anna@example.com' };\n  const product = { name: 'Laptop', price: 999.99 };\n  \n  return (\n    <div>\n      <h2>HOC Examples</h2>\n      \n      <UserProfileWithLogger user={user} />\n      <UserProfileWithLoading isLoading={true} user={user} />\n      <UserProfileWithLoading isLoading={false} user={user} />\n      \n      <EnhancedUserProfile \n        user={user} \n        isLoading={false}\n      />\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Типичные случаи использования HOC: 1) Логирование действий и рендеров; 2) Обработка ошибок (error boundaries); 3) Контроль доступа (authentication, authorization); 4) Подписка на внешние данные (data fetching); 5) Инъекция пропсов (injecting props); 6) Управление фокусировкой, формой и другими аспектами UI; 7) Интернационализация (i18n); 8) Аналитика и отслеживание событий."
      },
      {
        "type": "list",
        "items": [
          "HOC — функция, принимающая компонент и возвращающая новый компонент",
          "Имена HOC обычно начинаются с префикса 'with' (withRouter, withAuth)",
          "Не мутировать оборачиваемый компонент — создавать новый",
          "Передавать все пропсы в обёрнутый компонент",
          "Изолировать специфичные для HOC пропсы",
          "Устанавливать displayName для отладки в DevTools"
        ]
      },
      {
        "type": "code",
        "content": "// HOC для проверки аутентификации\nfunction withAuth(WrappedComponent, requiredRole = null) {\n  return function WithAuthComponent(props) {\n    // В реальном приложении аутентификация могла бы быть из контекста\n    const { isAuthenticated, user, login } = useAuth();\n    \n    // Проверяем аутентификацию\n    if (!isAuthenticated) {\n      return (\n        <div className=\"auth-required\">\n          <p>You need to be logged in to view this page.</p>\n          <button onClick={login}>Login</button>\n        </div>\n      );\n    }\n    \n    // Проверяем роль, если требуется\n    if (requiredRole && user.role !== requiredRole) {\n      return (\n        <div className=\"role-required\">\n          <h3>Access Denied</h3>\n          <p>You don't have permission to view this page.</p>\n          <p>Required role: {requiredRole}</p>\n          <p>Your role: {user.role}</p>\n        </div>\n      );\n    }\n    \n    // Если всё OK, рендерим обёрнутый компонент с дополнительным пропсом user\n    return <WrappedComponent {...props} user={user} />;\n  };\n}\n\n// HOC для загрузки данных\nfunction withDataFetching(urlPropName, dataPropName = 'data') {\n  return function(WrappedComponent) {\n    return function WithDataFetchingComponent(props) {\n      const [data, setData] = useState(null);\n      const [loading, setLoading] = useState(true);\n      const [error, setError] = useState(null);\n      \n      const url = props[urlPropName]; // Получаем URL из пропсов\n      \n      useEffect(() => {\n        if (!url) return;\n        \n        const fetchData = async () => {\n          try {\n            setLoading(true);\n            const response = await fetch(url);\n            \n            if (!response.ok) {\n              throw new Error(`HTTP error! status: ${response.status}`);\n            }\n            \n            const result = await response.json();\n            setData(result);\n            setError(null);\n          } catch (err) {\n            setError(err.message);\n            console.error('Fetch error:', err);\n          } finally {\n            setLoading(false);\n          }\n        };\n        \n        fetchData();\n      }, [url]);\n      \n      // Подготавливаем пропсы для обёрнутого компонента\n      const injectedProps = {\n        [dataPropName]: data,\n        loading,\n        error,\n        refetch: () => {\n          setLoading(true);\n          // Повторная загрузка данных\n        }\n      };\n      \n      // Если загрузка, показываем индикатор\n      if (loading) {\n        return <div className=\"loading-spinner\">Loading...</div>;\n      }\n      \n      // Если ошибка, показываем сообщение\n      if (error) {\n        return (\n          <div className=\"error-message\">\n            <h3>Error loading data</h3>\n            <p>{error}</p>\n            <button onClick={() => setLoading(true)}>Retry</button>\n          </div>\n        );\n      }\n      \n      // Рендерим обёрнутый компонент с дополнительными пропсами\n      return <WrappedComponent {...props} {...injectedProps} />;\n    };\n  };\n}\n\n// Компоненты\nfunction UserDetails({ user, loading, error }) {\n  if (loading) return <p>Loading user...</p>;\n  if (error) return <p>Error: {error}</p>;\n  \n  return (\n    <div>\n      <h2>{user.name}</h2>\n      <p>Email: {user.email}</p>\n    </div>\n  );\n}\n\nfunction AdminPanel({ user }) {\n  return (\n    <div className=\"admin-panel\">\n      <h2>Admin Panel</h2>\n      <p>Welcome, Admin {user.name}!</p>\n    </div>\n  );\n}\n\n// Применение HOC\nconst UserDetailsWithData = withDataFetching('apiUrl', 'user')(UserDetails);\nconst AdminPanelWithAuth = withAuth(AdminPanel, 'admin');\n\n// Использование\nfunction Dashboard() {\n  return (\n    <div>\n      <UserDetailsWithData \n        apiUrl=\"https://api.example.com/users/123\"\n      />\n      \n      <AdminPanelWithAuth />\n    </div>\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "Конвенции и лучшие практики для HOC: 1) Начинать имя HOC с префикса with (withRouter, withAuth); 2) Не мутировать оборачиваемый компонент, а создавать новый; 3) Передавать все пропсы в обёрнутый компонент (использовать spread оператор); 4) Изолировать специфичные для HOC пропсы от пропсов оборачиваемого компонента; 5) Устанавливать displayName для отладки; 6) Использовать статические методы правильно (hoist-non-react-statics)."
      },
      {
        "type": "code",
        "content": "// Продвинутый HOC с поддержкой статических методов\nimport hoistNonReactStatics from 'hoist-non-react-statics';\n\nfunction withAnalytics(WrappedComponent, eventName) {\n  class WithAnalyticsComponent extends React.Component {\n    // Статическое свойство\n    static displayName = `WithAnalytics(${WrappedComponent.displayName || WrappedComponent.name})`;\n    \n    componentDidMount() {\n      // Отправка события аналитики\n      this.trackEvent('component_mounted');\n    }\n    \n    componentDidUpdate(prevProps) {\n      // Отслеживание изменений пропсов\n      if (this.props.userId !== prevProps.userId) {\n        this.trackEvent('user_changed');\n      }\n    }\n    \n    trackEvent = (action) => {\n      // Имитация отправки в аналитику\n      console.log(`Analytics: ${eventName}_${action}`, {\n        component: WrappedComponent.displayName || WrappedComponent.name,\n        props: this.props,\n        timestamp: new Date().toISOString()\n      });\n    };\n    \n    handleClick = (e) => {\n      // Перехват кликов\n      this.trackEvent('click');\n      \n      // Вызов оригинального обработчика, если есть\n      if (this.props.onClick) {\n        this.props.onClick(e);\n      }\n    };\n    \n    render() {\n      // Фильтруем специфичные пропсы HOC\n      const { analyticsId, ...restProps } = this.props;\n      \n      return (\n        <WrappedComponent\n          {...restProps}\n          onClick={this.handleClick}\n          analyticsId={analyticsId || 'default'}\n        />\n      );\n    }\n  }\n  \n  // Копируем статические методы с оборачиваемого компонента\n  hoistNonReactStatics(WithAnalyticsComponent, WrappedComponent);\n  \n  return WithAnalyticsComponent;\n}\n\n// Компонент со статическими методами\nclass ProductCard extends React.Component {\n  static defaultProps = {\n    variant: 'default'\n  };\n  \n  static getProductCategory(productId) {\n    // Статический метод\n    return productId > 100 ? 'premium' : 'standard';\n  }\n  \n  render() {\n    const { product, onClick } = this.props;\n    \n    return (\n      <div className=\"product-card\" onClick={onClick}>\n        <h3>{product.name}</h3>\n        <p>${product.price}</p>\n      </div>\n    );\n  }\n}\n\n// Применение HOC (статические методы сохраняются)\nconst ProductCardWithAnalytics = withAnalytics(ProductCard, 'product_card');\n\n// Статические методы доступны\nconsole.log(ProductCardWithAnalytics.defaultProps); // { variant: 'default' }\nconsole.log(ProductCardWithAnalytics.getProductCategory(150)); // 'premium'\n\n// Использование\nfunction ProductListing() {\n  const product = { id: 123, name: 'Laptop', price: 999.99 };\n  \n  return (\n    <ProductCardWithAnalytics\n      product={product}\n      analyticsId=\"homepage\"\n      onClick={() => console.log('Card clicked')}\n    />\n  );\n}"
      },
      {
        "type": "paragraph",
        "content": "HOC vs Render Props vs Custom Hooks: HOC были основным способом повторного использования логики до появления хуков. Render Props — другой паттерн, где компонент принимает функцию как пропс и вызывает её для рендеринга. Кастомные хуки (Custom Hooks) — современная альтернатива, которая часто проще и понятнее, особенно для функциональных компонентов."
      }
    ]
  }
]
